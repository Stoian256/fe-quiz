import logo from "./../assets/header-logo-white.svg";

const Logo = () => {
  return (
    <div className="p-6 pl-2 mr-0">
      <img src={logo} alt={logo} />
    </div>
  );
};

export default Logo;
